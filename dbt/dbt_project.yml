name: "marketplace_sales"
version: "1.0.0"
config-version: 2

profile: "marketplace_sales"

model-paths: ["models"]
macro-paths: ["macros"]
test-paths: ["tests"]
seed-paths: ["seeds"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  marketplace_sales:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
